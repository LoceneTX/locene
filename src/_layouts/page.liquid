<!doctype html>
<html lang="en" class="page-{{ page.fileSlug }}">
  {% render 'head.liquid' %}
  <body>
    <article class="page">
      <header>
        <a href="/" title="home">{% svg 'media/locene-logo.svg' %}</a>
        {% render 'divider.liquid' %}
      </header>
      {{ content }}
    </article>
  </body>
</html>
